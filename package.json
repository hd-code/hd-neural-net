{
    "name": "hd-neural-net",
    "version": "0.0.1",
    "private": true,
    "files": [
        "build"
    ],
    "main": "build/main.js",
    "scripts": {
        "prepare": "npm run ts",
        "start": "npm run ts && node test/test.js",
        "test": "npm run ts && mocha",
        "ts": "rm -rf build/* && tsc",
        "ts:debug": "rm -rf build/* && tsc --sourceMap"
    },
    "devDependencies": {
        "mocha": "^6.2.0",
        "typescript": "^3.5.3"
    }
}
