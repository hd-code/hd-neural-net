{
    "name": "hd-neural-net",
    "version": "1.0.0",
    "description": "A powerful node module, which lets you create, train and run calculations on a neural net. It uses a simple public interface and is lightweight and fast.",
    "files": [
        "build"
    ],
    "main": "build/main.js",
    "scripts": {
        "minify": "for i in $(find ./build -name *.js); do uglifyjs $i -o $i -mc --comments '/^!/'; done",
        "prepare": "npm run ts && npm run minify",
        "start": "npm test",
        "test": "npm run prepare && mocha $(find ./test -type f)",
        "ts": "rm -rf build/* && tsc",
        "ts:debug": "rm -rf build/* && tsc --sourceMap"
    },
    "keywords": [
        "neural-net",
        "ai",
        "self-learning",
        "machine-learning"
    ],
    "author": "Hannes Dröse",
    "license": "UNLICENSED",
    "devDependencies": {
        "mocha": "^7.0.0",
        "typescript": "^3.7.5",
        "uglify-js": "^3.7.5"
    }
}
