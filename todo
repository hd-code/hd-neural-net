Before release:
[x] unit test: init, calc
    [x] init -> 2,2  3,7,5,4  bias – noBias
             -> 2,2,2,2 actFunc configs
    [x] calc -> one example net
[+] unit test train
    [x] init, train, calc -> x2              – 1,1 net
    [x] init, train, calc -> AND OR XOR XNOR – 2,8,4 net
[ ] isValid function -> net, input and TrainData
[ ] unit test isValid
    [ ] invalid net
    [ ] in- and outputs don't match
    [ ] wrong number of inputs
    [ ] wrong number of outputs
    [ ] valid net, input and output
[ ] JSDoc on all public functions
[ ] README file

General:
[ ] better testing framework?

New Features:
- cli with saving neural net as json file to disk
- convolutional layers
- pooling layers